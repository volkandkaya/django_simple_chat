============
Installation
============

At the command line::

    $ easy_install django_simple_chat

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_simple_chat
    $ pip install django_simple_chat
